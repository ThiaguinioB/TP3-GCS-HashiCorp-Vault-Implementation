apiVersion: apps/v1
kind: Deployment
metadata:
  name: demo-v1
  labels: { app: demo, version: v1 }
spec:
  replicas: 2
  selector:
    matchLabels: { app: demo, version: v1 }
  template:
    metadata:
      labels: { app: demo, version: v1 }
    spec:
      containers:
        - name: color-app
          image: color-app:latest          # o <tu_usuario>/color-app:1.0
          imagePullPolicy: IfNotPresent
          env:
            - name: VERSION
              value: "v1"
            - name: PORT
              value: "8080"
          ports:
            - containerPort: 8080
          readinessProbe:
            httpGet: { path: "/", port: 8080 }
            initialDelaySeconds: 2
            periodSeconds: 3
